const NotFoundPage = () => {
  return (
    <div style={{
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
    }}>
      <h2 style={{ margin: 0, color: "appColor.aboutPageText" }}>
        Page not found
      </h2>
    </div>
  );
};

export default NotFoundPage;
